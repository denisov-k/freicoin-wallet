# Freiland Registry — MVP-спецификация (релей-версия, без форка)

Второй столп Гезелля в крипте: дефицитные слоты (для MVP — **человекочитаемые
имена**) держатся не в вечной собственности, а «в аренду у общины». Рента — не
приватному лендлорду, а через демерредж. Никакого сквоттинга: у любого имени
всегда есть цена, по которой его можно выкупить.

Строится на существующем nv3 (user-токены + DEX + present-value + релей) с той же
моделью доверия, что и наши свопы: **релей индексирует и хостит доску, но не может
украсть** — токен и залог двигаются только подписями, которые кошелёк строит и
проверяет сам. Полная бесключевая (ковенант-)версия — отдельный софтфорк потом.

---

## 1. Одно число решает всё

Каждая регистрация описывается **единственной самозаявленной величиной `V`**
(в FRC). Из неё вытекает и цена, и рента:

- **`V` = цена принудительной продажи.** Любой платит `V` — забирает имя.
- **`V` = база ренты.** Держатель бондирует депозит ≈ `V`; демерредж этого депозита
  (≈5.09 %/год по консенсусу Freicoin) **и есть земельная рента**. Отдельного
  «платежа за аренду» нет — деньги «ржавеют» сами, ровно по Гезеллю.

Заявишь `V` низко — заплатишь мало ренты, но имя дёшево выкупят. Заявишь высоко —
имя защищено, но депозит тает быстрее. Гармония Харбергера, и вся рента —
«бесплатно» из уже работающего демерреджа. **Ставку ренты задаёт консенсус
(демерредж), не мы** — это ограничение, но оно же и есть гезеллевская ставка.

**Минимум `V` — по Гезеллю (2026-07-23).** Никакого капитального ценза: у Гезелля
Freiland — универсальный доступ, а сквоттинг сдерживают самооценка + демерредж, не
барьер входа. Единственный пол — ТЕХНИЧЕСКИЙ dust-порог (уступка арифметике, Фриденбах):
ниже ~сотни карий демерредж-залог начинает мусорить округлением / упирается в freeze-зону
1-юнитных монет ([[one-unit-coin-freeze]]). Дефолт `LAND_MIN_V = 0.01 FRC` — на ~6 порядков
выше freeze-зоны, на ~4 ниже прежней сотни; это политика релея (отдаётся как `minV`,
клиент читает оттуда), не консенсус. Чего ещё не хватает до «чистого Гезелля» — рента в
общинную казну вместо майнеров (demurrage-treasury), но это консенсус-изменение (§7).

---

## 2. On-chain объекты (nv3, как есть)

1. **Имя = уникальный именованный токен.** nv3 уже умеет unique-named-items
   (токены, что не тают, ходят целиком, имена не повторяются — см. память
   *smart-property tokens*). Токен с именем `alice` = владение слотом `alice`.
   Держатель токена = держатель слота.
2. **Rent-депозит** — FRC, запертые в оффер-скрипте (наш ranged/HTLC-шаблон), так
   что (а) тают демерреджем (рента), (б) забираются покупателем атомарно при выкупе.
3. **Резолвер-запись** — куда указывает имя (адрес получателя). Хранится в witness-
   extension выхода токена (как assetTag-миграция уже делает) или в самом токене.

Депозит бондируется ≈ `V` (его годовой демерредж ≈ ставка × `V` = рента). Точная
формула привязки `депозит → V` (учёт уже стаявшего, порог делинквентности) —
единственная деталь бухгалтерии, которую надо зафиксировать в реализации.

---

## 3. Потоки

**Регистрация.** Выбрал свободное имя → задал `V` → выпустил именной токен (issue
на nv3) → запер депозит ≈ `V` → выставил на релее **постоянный оффер продажи**
токена по цене `V` (существующий ranged-оффер). Имя «зарегистрировано», пока живы
депозит и стоящий оффер.

**Держание.** Депозит тает (это рента). Present-value депозита сползает — значит и
твоя заявленная `V` сползает, если не пополнять. Забросил имя → его `V` и рента
дрейфуют вниз → его дёшево выкупают. **Сквоттеры вычищаются автоматически.** Хочешь
поднять цену/защиту — доливаешь депозит и перевыставляешь оффер на новую `V`.

**Принудительная продажа (forced sale).** Покупатель исполняет твой стоящий оффер:
атомарно платит `V` FRC → получает именной токен. Твой депозит разблокируется тебе
(минус стаявшее). Покупатель тут же регистрирует заново: своя `V`, свой депозит,
свой оффер. Это **буквально наш ranged-оффер/HTLC-своп**, где «активом» выступает
имя-токен. Никакого нового кода механики — переиспользование `core/dex.mjs` +
`swap-drive.mjs`.

**Резолв (польза уже сейчас).** Кошелёк резолвит `имя → адрес`: отправляешь на
`alice` вместо `fc1q…`. Это немедленная утилита (и повод пользоваться) поверх
чистой Freiland-механики.

---

## 4. Релей (market-server.mjs — доска, не хранилище ценности)

Новые read-модели поверх существующего p2p-стора:
- `landRegister({name, ownerPub, V, offerId, depositOutpoint, resolve})` — заявка
  регистрации (валидирует уникальность имени, наличие оффера и депозита).
- `landList / landLookup(name)` — индекс имён: держатель, `V`, стоящий оффер,
  present-value депозита, статус (живой / делинквентный).
- Выкуп идёт через уже существующие take/HTLC-эндпоинты DEX — отдельного «купить
  имя» не нужно, это ranged-тейк токена.

Доверие: релей может спрятать имя или оффер (цензура), но не подменить владельца —
токен и депозит двигаются только валидными подписями кошелька. Гарантия
«всегда-в-продаже» так же сильна, как то, что релей показывает стоящий оффер;
для MVP приемлемо (как весь наш DEX), апгрейд до ковенанта — потом.

## 5. Кошелёк (вкладка внутри существующего Биржа-флоу, по правилу UI-integration)

- **Найти/зарегистрировать имя** — поле имени, задать `V`, «Зарегистрировать»
  (issue токена + депозит + оффер одним визардом).
- **Мои имена** — список с `V`, present-value депозита, «долить»/«поднять цену».
- **Выкупить** — на чужом имени: «Купить за `V`» = ranged-тейк (уже есть).
- **Резолв** — в «Отправить»: ввёл `alice` → подставился адрес (переиспользует поле).

---

## 6. Что переиспользуем (≈70 % готово)

| Нужно | Уже есть в nv3 |
|---|---|
| Слот-маркер | unique-named tokens (issue, ходят целиком, имена уникальны) |
| Рента | демерредж + `assetPresentValue` (сам тает) |
| Всегда-в-продаже | ranged/pre-signed офферы (`core/dex.mjs`) |
| Атомарный выкуп | HTLC-своп механика (`swap-drive.mjs`) |
| Индекс/доска | релей p2p-стор + ордербук |
| UI | вкладка Биржа + форма «Отправить» (резолв) |

Нового: `land*`-эндпоинты релея (тонкая обёртка над p2p-стором), визард
регистрации в кошельке, резолвер `имя→адрес`, формула `депозит↔V`.

## 7. Явно вне MVP

- **Ковенант-версия** (forced-sale вшит в выход, без релея/пере-подписи) —
  консенсус-изменение на nv3, софтфорк; делать после доказанного спроса.
- **Demurrage-казна** (рента → прозрачный commons вместо майнеров) — тоже
  консенсус-изменение (меняет получателя вечной субсидии); отдельный, более
  тяжёлый шаг.
- Мульти-неймспейсы (тикеры, «участки», слоты) — та же механика, позже.
- Аукцион при первичной раздаче имён — можно добавить (nv3 умеет аукционы).

## 8. Порядок сборки

1. Формула `депозит↔V` + статус делинквентности (чистая функция + тесты).
2. `land*`-эндпоинты релея над p2p-стором; уникальность имён.
3. Визард регистрации в кошельке (issue+deposit+offer одной кнопкой).
4. Резолвер `имя→адрес` в «Отправить».
5. Выкуп = ranged-тейк токена (проводка существующего пути на имена).
6. «Мои имена» + долив/переоценка.

MVP = первый честный Freiland в крипте на готовой инфраструктуре, без форка.
Дистрибуционный крючок: «Gesell's Freiland + Harberger on-chain, живьём».

## 9. Шаг 5 — трастлесс-выкуп: решение по модели (2026-07-23)

Ключевое ограничение, обнаруженное при сборке: **DEX запрещает same-asset
ranged-оффер** (`core/dex.mjs:223`: `payout.assetId === giveAsset` → throw).
Значит «залог-как-оффер» (FRC↔FRC) невозможен; имя ОБЯЗАНО быть отдельным от
FRC-платежа nv3-объектом, чтобы `SIGHASH_BUNDLE` связал *give токен ↔ want V FRC
владельцу*. Релей-опосредованный выкуп отвергнут (платит-покупатель→релей-
переназначает = «украсть цензурой», спека это запрещает).

**Выбрана Model A — NFT-токен + FRC-залог** (не «тающий пер-нейм актив»):
у Model B фиксированный супплай ⇒ поднять самооценку V нельзя, а Гарбергеру
восходящая переоценка НУЖНА (защищать дорожающее имя). Это вырезало бы половину
механизма — поэтому A, хоть и сложнее (два объекта).

Модель A:
- **Владение** = уникальный NFT-токен `land:<name>` (1 штука, nv3-актив,
  tag = hash160(def) коммитит имя ⇒ выпуск ровно один раз, ре-иссью заблокирован).
- **Ценность/рента** = отдельный тающий FRC-залог; V = самооценка (цена продажи).
  Релей требует present value залога ≥ V (иначе имя недо-обеспечено → выкупаемо/
  истекает). Долив залога = уплата ренты (держать PV ≥ V по мере таяния).
- **Стоячий оффер**: NFT ⇄ V FRC → владельцу, цена фикс = V (пере-ставится только
  при СМЕНЕ V, не ежеблочно; залог отдельно трекает демерредж доливом).

Карта переиспользования (всё уже есть, шаг 5 — оркестровка):
- mint NFT → `api('issue', {name:'land:<n>', amount:1, tokens:['<n>'], interest:false,
  shift:64, spk: land-адрес})` (relay `issue`, tag уникален).
- стоячий оффер → token-ветка `postRangedOffer` (min=max=1, whole; `signLadder`/
  `signRangedGive` land-КЛЮЧОМ, не `km` — нужен рефактор подписи под произвольный ключ).
- **выкуп** = `fillRangedNow(offer, 1)` — УЖЕ умеет token give-coin (two-sided reveal),
  платит V FRC maker'у, NFT → покупателю. Далее покупатель ре-регистрирует (свой залог+оффер).
- relay `landRegister`: принять `nftTag`+`selfValue`; если nftTag задан — проверить
  что монета с этим токеном лежит на адресе владельца (`spkIndex`); enforce залог PV ≥ V.
  `landList/lookup`: отдать `nftTag`, `price`(V), `buyable`.

Стадии сборки шага 5 (каждая тестируется нодовым сьютом на fw-bdev):
  5a. ✅ relay-схема: land-запись несёт nftTag+selfValue, buyable/price в list/lookup (back-compat).
  5b. ✅ registerName: mint NFT → lock deposit → post standing offer → landRegister(nftTag,V).
      Реализация: ranged-подпись обобщена под произвольный ключ (signRangedGive/signLadder →
      swap-lib.mjs, параметр sec) — стоячий оффер подписывает LAND-ключ; тег NFT считается
      локально (nftDef повторяет релейный issue-def, слайс имени до 30); залог = V + неделя
      ренты (свежий залог тает — без запаса V > PV уже через блок); maybeResignLand держит
      лестницу свежей и перевыставляет истёкший оффер по min(V, PV залога) — самооценка
      сползает с тающим залогом (троттл перевыставления 10 мин/имя: книга info обрезана до 80).
  5c. ✅ (e2e на живом nv3 2026-07-23: BUY PASS + SWEEP PASS — полный цикл выкупа)
      buyName = fillRangedNow(offer, целиком, fillSpk = land-адрес покупателя) → adoptName
      (реюз/залог → свой оффер → перерегистрация) → свип залога продавцом (maybeResignLand,
      sweepDeposit land-ключом; выручка V приходит оффером на главный адрес автоматически).
      Находки живого e2e (все три ловились ТОЛЬКО на реальной цепи):
      ① NFT НЕЛЬЗЯ чеканить в 1 юнит: консенсусный asset_pv (tx_verify.cpp:
        TimeAdjustValueForwardK) усечён вниз ⇒ PV(1) = 0 на дистанции 1 при любом shift —
        монета замерзает навсегда (bad-txns-in-belowout). Фикс: NFT = 10^8 юнитов (NFT_UNITS);
        сумма в def не входит — тег не меняется. Более того, kernel съедает РОВНО 1 юнит на
        любой дистанции ≥ 1 (floor) ⇒ minFill = units−1, не units (ниже нельзя: ranged-подпись
        не пиннит токены — частичный филл увёз бы имя за долю цены; units−1 = вечный PV-пол,
        недоплата покупателя 10^-8·V). Тот же 1-юнитный баг ЛАТЕНТНО живёт в продаже тикетов
        по одному (splitToTicketCoins) — на регтесте прячется за майнингом в тот же блок.
      ② RBF-гонка: залог adoptName, собранный по устаревшему списку монет, тратил платёжную
        монету 0-fee филла и ВЫТЕСНЯЛ его из мемпула (залог платит комиссию). Фикс:
        bondAndRegister сначала ждёт NFT (= подтверждение филла/минта), потом шлёт залог;
        плюс идемпотентный реюз уже лежащего на land-адресе залога с PV ≥ V (ретраи).
      ③ Change целикового филла (0 юнитов, но с тегом имени) едет на land-адрес продавца и
        маскировал «NFT ушла» — детект NFT везде требует value > 0.
      Релейный фикс передачи: «имя занято» для Model A-записи решает ВЛАДЕНИЕ NFT (претендент
      предъявляет монету-имя на своём адресе; supply 1 ⇒ двух живых копий не бывает), а не
      живость старого залога — иначе выкуп блокировался бы, пока продавец не заберёт залог.
      Релей помечает оффер «filled» оптимистично при броадкасте — невзлетевший филл лечится
      перевыставлением со стороны кошелька владельца (maybeResignLand), проверено e2e.
  5d. UI: «Купить за V» — СДЕЛАНО вместе с 5c (кнопка Buy на доске, prompt новой самооценки,
      fillRangedNow с fillSpk + adoptName).

Шаг 6 (переоценка/долив) — ✅ (e2e на живом nv3 2026-07-23: 120→150 PASS). `revalueName`:
залог пересобирается ОДНИМ tx (старый залог подписан land-ключом, при нехватке добираются
монеты кошелька; свежий залог = V′ + неделя ренты, излишек/сдача в кошелёк) → старый оффер
снимается ДО нового (при подъёме V дешёвый оффер не должен висеть; окно «не купить» — секунды,
страховка — перевыставление maybeResignLand) → новый оффер по V′ → перерегистрация (общий
финал offerAndRegister). UI: кнопка ± в «Мои имена» (строка показывает V / залог). Подъём V =
долив (рента вперёд), спуск V = вернуть излишек себе — обе стороны Харбергера.
Каждая стадия — отдельный коммит с прогоном нодового сьюта (это money-path, не UI).
